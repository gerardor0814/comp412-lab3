//NAME: Gerardo Ramirez del Rio
//NETID: gr29

To build program run:
    make clean
    make build

In the directory with the Makefile

Flags supported:
    Required arguments:
        filename  is the pathname (absolute or relative) to the input file

    Optional flag:
            -h        prints this message
